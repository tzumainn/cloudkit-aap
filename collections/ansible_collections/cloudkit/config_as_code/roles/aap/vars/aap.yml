---
aap_organizations: # noqa: var-naming[no-role-prefix]
  - name: "{{ aap_organization_name }}"

aap_user_accounts: # noqa: var-naming[no-role-prefix]
  # create a user for EDA
  - username: "{{ aap_eda_service_user_name }}"
    is_superuser: false
    password: "{{ aap_eda_service_user_password }}"
    update_secrets: false

controller_roles: # noqa: var-naming[no-role-prefix]
  # Here we allow EDA to trigger the job templates we want
  - user: "{{ aap_eda_service_user_name }}"
    lookup_organization: "{{ aap_organization_name }}"
    role: "execute"
    job_templates:
      - "{{ aap_prefix }}-create-hosted-cluster"
      - "{{ aap_prefix }}-delete-hosted-cluster"
      - "{{ aap_prefix }}-create-hostpool"
      - "{{ aap_prefix }}-delete-hostpool"
      - "{{ aap_prefix }}-create-host"
      - "{{ aap_prefix }}-delete-host"
      - "{{ aap_prefix }}-create-compute-instance"
      - "{{ aap_prefix }}-delete-compute-instance"
      - "{{ aap_prefix }}-report-hosted-cluster-status-success"
      - "{{ aap_prefix }}-report-hosted-cluster-status-failure"
      - "{{ aap_prefix }}-report-hostpool-status-success"
      - "{{ aap_prefix }}-report-hostpool-status-failure"
      - "{{ aap_prefix }}-report-host-status-success"
      - "{{ aap_prefix }}-report-host-status-failure"
    workflows:
      - "{{ aap_prefix }}-create-hosted-cluster-workflow"
      - "{{ aap_prefix }}-delete-hosted-cluster-workflow"
      - "{{ aap_prefix }}-create-hostpool-workflow"
      - "{{ aap_prefix }}-delete-hostpool-workflow"
      - "{{ aap_prefix }}-create-host-workflow"
      - "{{ aap_prefix }}-delete-host-workflow"
