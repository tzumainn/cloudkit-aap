- name: Get remote cluster kubeconfig
  ansible.builtin.set_fact:
    remote_cluster_kubeconfig: "{{ lookup('env', 'OSAC_REMOTE_CLUSTER_KUBECONFIG') }}"
  when: lookup('env', 'OSAC_REMOTE_CLUSTER_KUBECONFIG') | length > 0
