#!/usr/bin/python
# -*- coding: utf-8 -*-
# This file is auto-generated by scripts/generate.py. Do not edit manually.

# SPDX-FileCopyrightText: Copyright (c) 2026 Fabien Dupont
# SPDX-License-Identifier: Apache-2.0

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: nvidia.bare_metal.allocation_constraint
short_description: Manage allocation_constraint resources
description:
- Manage allocation_constraint resources
version_added: 1.0.0
author: NVIDIA Bare Metal Manager Dev Team
extends_documentation_fragment:
- nvidia.bare_metal.auth
options:
  allocation_constraint_id:
    type: str
    description:
    - 'ID path parameter: allocation_constraint_id.'
  allocation_id:
    type: str
    description:
    - 'Scope filter: allocation_id.'
  constraint_type:
    type: str
    description:
    - constraint_type parameter.
    choices:
    - Reserved
    - OnDemand
    - Preemptible
  constraint_value:
    type: int
    description:
    - constraint_value parameter.
  id:
    type: str
    description:
    - ID of the resource. Used for lookup.
  resource_type:
    type: str
    description:
    - resource_type parameter.
    choices:
    - InstanceType
    - IPBlock
  resource_type_id:
    type: str
    description:
    - resource_type_id parameter.
  state:
    type: str
    description:
    - Desired state of the resource.
    choices:
    - present
    - absent
  wait:
    type: bool
    description:
    - Wait for the resource to reach the desired state.
  wait_timeout:
    type: int
    description:
    - Timeout in seconds for wait operations.
'''

EXAMPLES = r'''
---
- name: Create a allocation_constraint
  nvidia.bare_metal.allocation_constraint:
    api_url: "{{ api_url }}"
    api_token: "{{ api_token }}"
    org: "{{ org }}"
    state: present
    name: "my-allocation-constraint"

- name: Delete a allocation_constraint
  nvidia.bare_metal.allocation_constraint:
    api_url: "{{ api_url }}"
    api_token: "{{ api_token }}"
    org: "{{ org }}"
    state: absent
    name: "my-allocation-constraint"
'''

RETURN = r'''
---
resource:
    description: The resource details.
    type: dict
    returned: when state is present
'''

from ansible.module_utils.basic import AnsibleModule
from ansible_collections.nvidia.bare_metal.plugins.module_utils.common import get_auth_argument_spec
from ansible_collections.nvidia.bare_metal.plugins.module_utils.resource import CrudResource


ARGUMENT_SPEC = dict(
allocation_constraint_id=dict(type='str'),
allocation_id=dict(type='str'),
constraint_type=dict(type='str', choices=['Reserved', 'OnDemand', 'Preemptible']),
constraint_value=dict(type='int'),
id=dict(type='str'),
resource_type=dict(type='str', choices=['InstanceType', 'IPBlock']),
resource_type_id=dict(type='str'),
state=dict(type='str', choices=['present', 'absent']),
wait=dict(type='bool'),
wait_timeout=dict(type='int'),
)

RESOURCE_CONFIG = {
    'resource_path': '/v2/org/{org}/carbide/allocation/{allocationId}/constraint',
    'resource_item_path': '/v2/org/{org}/carbide/allocation/{allocationId}/constraint/{allocationConstraintId}',
    'id_param': 'allocationConstraintId',
    'name_field': None,
    'create_schema_fields': ['resource_type', 'resource_type_id', 'constraint_type', 'constraint_value'],
    'update_schema_fields': ['constraint_value'],
    'scope_fields': ['allocation_id'],
    'ready_statuses': ['Ready'],
    'error_statuses': ['Error'],
    'no_create': False,
    'delete_body_fields': [],
    'version_field': None,
}


def main():
    auth_spec = get_auth_argument_spec()
    auth_spec.update(ARGUMENT_SPEC)
    module = AnsibleModule(argument_spec=auth_spec, supports_check_mode=True)
    CrudResource(module, RESOURCE_CONFIG).run()


if __name__ == "__main__":
    main()
