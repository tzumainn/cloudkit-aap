---
# Integration test for osac.service.cluster_working_namespace role.
#
# Play order matters: "not found" runs first (before any fact is set),
# "pre-defined" second, "found" last. Ansible set_fact persists across
# plays within a single invocation, so the "found" test must come last
# to avoid polluting subsequent plays.

# ── Test 1: Namespace not found (expected failure) ──
# Runs first so cluster_working_namespace is genuinely undefined.
- name: "ClusterWorkingNamespace - Test not found (should fail)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../../../common_vars.yml

  tasks:
    - name: Run not-found failure test
      block:
        - name: Run role with nonexistent order name
          ansible.builtin.include_role:
            name: osac.service.cluster_working_namespace
          vars:
            cluster_working_namespace_cluster_order_name: "nonexistent-order"

        - name: This should not be reached
          ansible.builtin.fail:
            msg: "Role did not fail when namespace was not found"

      rescue:
        - name: Verify role failed with expected message
          ansible.builtin.assert:
            that:
              - "'Working cluster namespace was not found' in ansible_failed_result.msg"
            fail_msg: "Role failed but with unexpected message: {{ ansible_failed_result.msg }}"
            success_msg: "Not found failure: passed"

# ── Test 2: Pre-defined preserved ──
- name: "ClusterWorkingNamespace - Test pre-defined preserved"
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../../../common_vars.yml

  tasks:
    - name: Pre-define cluster_working_namespace
      ansible.builtin.set_fact:
        cluster_working_namespace: "my-custom-namespace"

    - name: Run role (should skip lookup)
      ansible.builtin.include_role:
        name: osac.service.cluster_working_namespace
      vars:
        cluster_working_namespace_cluster_order_name: "does-not-matter"

    - name: Verify pre-defined value preserved
      ansible.builtin.assert:
        that:
          - cluster_working_namespace == "my-custom-namespace"
        fail_msg: "Pre-defined namespace was overwritten"
        success_msg: "Pre-defined preserved: passed"

# ── Test 3: Namespace found by label ──
# Runs last because set_fact persists across plays.
- name: "ClusterWorkingNamespace - Test namespace found by label"
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../../../common_vars.yml

  tasks:
    - name: Test namespace found
      block:
        - name: Create labeled test namespace
          kubernetes.core.k8s:
            state: present
            definition:
              apiVersion: v1
              kind: Namespace
              metadata:
                name: cluster-cwn-test-work
                labels:
                  osac.openshift.io/clusterorder: "cwn-test-order"

        - name: Run cluster_working_namespace role
          ansible.builtin.include_role:
            name: osac.service.cluster_working_namespace
          vars:
            cluster_working_namespace_cluster_order_name: "cwn-test-order"

        - name: Verify namespace resolved
          ansible.builtin.assert:
            that:
              - cluster_working_namespace is defined
              - cluster_working_namespace == "cluster-cwn-test-work"
            fail_msg: "Expected 'cluster-cwn-test-work', got '{{ cluster_working_namespace | default('undefined') }}'"
            success_msg: "Namespace found by label: passed"

      always:
        - name: Cleanup -- delete test namespace
          kubernetes.core.k8s:
            state: absent
            api_version: v1
            kind: Namespace
            name: cluster-cwn-test-work
