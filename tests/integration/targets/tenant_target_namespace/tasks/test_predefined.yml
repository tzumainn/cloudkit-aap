---
- name: "TenantTargetNamespace - Test pre-defined preserved"
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../../../common_vars.yml

  tasks:
    - name: Pre-define tenant_target_namespace
      ansible.builtin.set_fact:
        tenant_target_namespace: "my-custom-vm-namespace"

    - name: Run role (should skip lookup)
      ansible.builtin.include_role:
        name: osac.service.tenant_target_namespace
      vars:
        compute_instance:
          status:
            tenantReference:
              name: does-not-matter
              namespace: osac-system

    - name: Verify pre-defined value preserved
      ansible.builtin.assert:
        that:
          - tenant_target_namespace == "my-custom-vm-namespace"
        fail_msg: "Pre-defined namespace was overwritten"
        success_msg: "Pre-defined preserved: passed"
